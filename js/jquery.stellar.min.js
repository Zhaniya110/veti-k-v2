
$(window).scroll(function() {
	const
		a = $(this).scrollTop(),
		b = 800;
	$(".front-page .top").css({
		top: a / 1.6 + "px",
		opacity: 1 - a / b
	});
});

// parallax scrolling

document.addEventListener("scroll", () => {

});
